int main() {
    int x;
    int a[3];

    x = (1 + 2;             // missing )

    a[0 = 1;                // missing ]

    x = ((x + 1) * 2;       // inner pair closed, outer one not

    if (x > 0) {            // this { never gets closed
        x = 1;

    return 0;
}
